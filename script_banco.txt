CREATE TABLE public.tb_categoria (
    id bigint NOT NULL,
    nome character varying(255)
);


ALTER TABLE public.tb_categoria ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.tb_categoria_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);



CREATE TABLE public.tb_pedido (
    id bigint NOT NULL,
    data timestamp without time zone,
    pedido_status integer,
    cliente_id bigint
);



ALTER TABLE public.tb_pedido ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.tb_pedido_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);



CREATE TABLE public.tb_pedido_item (
    preco double precision,
    quantidade integer,
    produto_id bigint NOT NULL,
    pedido_id bigint NOT NULL
);


CREATE TABLE public.tb_pagamento (
    dta_pagamento timestamp without time zone,
    pedido_id bigint NOT NULL
);


CREATE TABLE public.tb_produto (
    id bigint NOT NULL,
    descricao character varying(255),
    image_url character varying(255),
    nome character varying(255),
    preco double precision,
    dta_cadastro date
);


CREATE TABLE public.tb_produto_categoria (
    produto_id bigint NOT NULL,
    categoria_id bigint NOT NULL
);


ALTER TABLE public.tb_produto ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.tb_produto_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);


CREATE TABLE public.tb_usuario (
    id bigint NOT NULL,
    email character varying(255),
    name character varying(255),
    password character varying(255),
    phone character varying(255),
    dta_cadastro  date
);

ALTER TABLE public.tb_usuario ALTER COLUMN id ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.tb_usuario_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

ALTER TABLE ONLY public.tb_categoria
    ADD CONSTRAINT tb_categoria_pkey PRIMARY KEY (id);

ALTER TABLE ONLY public.tb_pedido_item
    ADD CONSTRAINT tb_pedido_item_pkey PRIMARY KEY (pedido_id, produto_id);

ALTER TABLE ONLY public.tb_pedido
    ADD CONSTRAINT tb_pedido_pkey PRIMARY KEY (id);

ALTER TABLE ONLY public.tb_pagamento
    ADD CONSTRAINT tb_pagamento_pkey PRIMARY KEY (pedido_id);

ALTER TABLE ONLY public.tb_produto_categoria
    ADD CONSTRAINT tb_produto_categoria_pkey PRIMARY KEY (produto_id, categoria_id);


ALTER TABLE ONLY public.tb_produto
    ADD CONSTRAINT tb_produto_pkey PRIMARY KEY (id);

ALTER TABLE ONLY public.tb_usuario
    ADD CONSTRAINT tb_usuario_pkey PRIMARY KEY (id);


ALTER TABLE ONLY public.tb_pedido_item
    ADD CONSTRAINT fk4h5xid5qehset7qwe5l9c997x FOREIGN KEY (produto_id) REFERENCES public.tb_produto(id);


ALTER TABLE ONLY public.tb_produto_categoria
    ADD CONSTRAINT fk5r4sbavb4nkd9xpl0f095qs2a FOREIGN KEY (categoria_id) REFERENCES public.tb_categoria(id);

ALTER TABLE ONLY public.tb_produto_categoria
    ADD CONSTRAINT fkgbof0jclmaf8wn2alsoexxq3u FOREIGN KEY (produto_id) REFERENCES public.tb_produto(id);



ALTER TABLE ONLY public.tb_pedido_item
    ADD CONSTRAINT fkgeobgl2xu916he8vhljktwxnx FOREIGN KEY (pedido_id) REFERENCES public.tb_pedido(id);


ALTER TABLE ONLY public.tb_pedido
    ADD CONSTRAINT fki0x0rv7d65vsceuy33km9567n FOREIGN KEY (cliente_id) REFERENCES public.tb_usuario(id);


ALTER TABLE ONLY public.tb_pagamento
    ADD CONSTRAINT fkokaf4il2cwit4h780c25dv04r FOREIGN KEY (pedido_id) REFERENCES public.tb_pedido(id);

